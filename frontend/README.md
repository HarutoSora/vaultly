# Vaultly — web app

The React/TypeScript web vault. See the [repo root README](../README.md) for
how to run the whole project (backend + this app + the extension), and
[`../docs/`](../docs) for the architecture and cryptography design.
